import { redirect } from 'next/navigation';

export default function WorldBankGroupAdminPage() {
    // Redirect to WorldBankGroup admin setup dashboard
    redirect('/worldbankgroup-admin/setup');
}
